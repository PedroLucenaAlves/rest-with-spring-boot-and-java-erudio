spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_erudio?useTimeZone=true&serverTimeZone=UTC
    username: root
    password: admin
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
   #  properties:   (comentado por conta do warning: MySQLDialect does not need to be specified explicitly using 'hibernate.dialect')
    #   hibernate:
     #     dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
